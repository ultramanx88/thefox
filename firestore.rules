rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // อนุญาตให้อ่านและเขียนได้ ถ้าคำขอนั้นเกิดขึ้นก่อนวันที่กำหนด
      allow read, write: if request.time < timestamp.date(2025, 8, 14);
    }
  }
}